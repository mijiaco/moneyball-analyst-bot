name: MFL trade poll

on:
  schedule:
    # Poll every 2 minutes (GitHub may start a few seconds late).
    - cron: "*/2 * * * *"
  workflow_dispatch:

permissions:
  contents: write

# One poll at a time so seen_trades.json and Discord posts stay consistent.
# Do not cancel an in-flight run when another is scheduled — that was delaying or dropping posts.
concurrency:
  group: mfl-trade-poll
  cancel-in-progress: false

jobs:
  poll:
    runs-on: ubuntu-latest
    env:
      DISCORD_BOT_TOKEN: ${{ secrets.DISCORD_BOT_TOKEN }}
      DISCORD_CHANNEL_ID: ${{ secrets.DISCORD_CHANNEL_ID }}
      MFL_API_KEY: ${{ secrets.MFL_API_KEY }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Require repository secrets
        run: |
          set -e
          for var in DISCORD_BOT_TOKEN DISCORD_CHANNEL_ID MFL_API_KEY; do
            if [ -z "${!var}" ]; then
              echo "::error::Missing Actions secret '$var'. Repo → Settings → Secrets and variables → Actions → New repository secret (name must match exactly)."
              exit 1
            fi
          done

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Poll and post trades
        run: python -m src.run_once

      # Avoid git push races (non-FF): update the file via REST with SHA + retries.
      - name: Upload seen_trades.json (Contents API)
        uses: actions/github-script@v8
        with:
          github-token: ${{ github.token }}
          script: |
            const fs = require("fs");
            const filePath = "data/seen_trades.json";
            if (!fs.existsSync(filePath)) {
              core.setFailed("missing " + filePath);
              return;
            }
            const newContent = fs.readFileSync(filePath, "utf8");
            const { owner, repo } = context.repo;
            const branch = context.ref.replace("refs/heads/", "");
            const b64 = Buffer.from(newContent, "utf8").toString("base64");

            for (let attempt = 1; attempt <= 10; attempt++) {
              let remoteSha = null;
              try {
                const { data } = await github.rest.repos.getContent({
                  owner,
                  repo,
                  path: filePath,
                  ref: branch,
                });
                if (Array.isArray(data)) {
                  throw new Error("path is a directory");
                }
                remoteSha = data.sha;
                const remoteText = Buffer.from(data.content, "base64").toString("utf8");
                if (remoteText === newContent) {
                  console.log("seen_trades.json unchanged on disk vs remote; skip commit");
                  return;
                }
              } catch (e) {
                if (e.status !== 404) {
                  throw e;
                }
              }

              try {
                await github.rest.repos.createOrUpdateFileContents({
                  owner,
                  repo,
                  path: filePath,
                  message: "chore: update seen trades dedupe state",
                  content: b64,
                  branch,
                  ...(remoteSha ? { sha: remoteSha } : {}),
                });
                console.log("Committed seen_trades.json via API");
                return;
              } catch (e) {
                const retry = (e.status === 409 || e.status === 422) && attempt < 10;
                if (retry) {
                  console.log(`API commit conflict (${e.status}), retry ${attempt}`);
                  await new Promise((r) => setTimeout(r, 4000));
                  continue;
                }
                throw e;
              }
            }
